<div class="container">
  <div class="row">
    <div class="col-sm-8">
      <div class="user_change_pass_password">
        <h3>Đổi mật khẩu mới</h3>
        <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
          <%= render "devise/shared/error_messages", resource: resource %>
          <%= f.hidden_field :reset_password_token %>

          <div class="user_reset_pass_content">
            <div class="user_reset_pass_label">
              <%= f.label :password, "Mật khẩu mới" %><br />
              <% if @minimum_password_length %>
                <em>(<%= @minimum_password_length %> characters minimum)</em><br />
              <% end %>
            </div>
            <div class="user_reset_pass_input">
              <%= f.password_field :password, autofocus: true, autocomplete: "new-password", class: "form-control" %>
            </div>
          </div>

          <div class="user_reset_pass_content">
            <div class="user_reset_pass_label">
              <%= f.label :password_confirmation, "Nhập lại mật khẩu mới" %><br />
            </div>
            <div class="user_reset_pass_input">
              <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control" %>
            </div>
          </div>

          <div class="user_reset_pass_submit">
            <%= f.submit "Đổi mật khẩu",  class: "user_reset_form_submit"%>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
