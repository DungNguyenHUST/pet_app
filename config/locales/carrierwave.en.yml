en:
  activerecord:
    errors:
      messages:
        record_invalid: 'Validation failed: %{errors}'
        restrict_dependent_destroy:
          has_one: Cannot delete record because a dependent %{record} exists
          has_many: Cannot delete record because dependent %{record} exist
  errors:
    messages:
      carrierwave_processing_error: unable to process
      carrierwave_integrity_error: not a valid file type
      carrierwave_download_error: unable to download
      extension_whitelist_error: " You cannot download files with format %{extension}, only files with format: %{allowed_types} "
      extension_blacklist_error: " You cannot download files with format %{extension}, do not allow downloading files with format: %{prohibited_types} "
      content_type_whitelist_error: " You cannot file with format %{content_type} "
      content_type_blacklist_error: " You cannot file with format %{content_type} "
      rmagick_processing_error: " Cannot process with rmagick, maybe your file is not an image? "
      mini_magick_processing_error: " Cannot process with MiniMagick, maybe your file is not an image? Original error: %{e} "
      min_size_error: " File size must be larger than %{min_size} "
      max_size_error: " File size must be less than %{max_size} "